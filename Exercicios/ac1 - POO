def eh_armstrong(n):
    n = int(input())
    potencia = len(str(n))
    soma = 0
    armstrong = n
    while armstrong >= 0:
        digitos = armstrong % 10
        soma = soma + (digitos ** potencia)
        armstrong = armstrong // 10
        n == armstrong
        return True
    return False


